namespace YUP.App.MediaPlayers
{
    /// <summary>
    /// Enum defines what media playback quality we are using
    /// </summary>
    public enum MediaPlaybackQuality
    {
        @default,
        small,
        medium,
        large,
        hd720,
        hd1080,
        highres
    }
}
